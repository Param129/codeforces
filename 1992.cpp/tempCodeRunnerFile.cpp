int n, k;
        // cin >> n >> k;

        // vector<int> a(k);
        // for (int i = 0; i < k; ++i) {
        //     cin >> a[i];
        // }

        // priority_queue<int, vector<int>, greater<int>> minHeap(a.begin(), a.end());
        // int operations = 0;

        // while (minHeap.size() > 1) {
        //     int smallest = minHeap.top(); minHeap.pop();
        //     int secondSmallest = minHeap.top(); minHeap.pop();

        //     int newPiece = smallest + secondSmallest;
        //     minHeap.push(newPiece);
        //     operations++;
        // }